#include<stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    long long int x, y, n;
    while(t--)
    {
        scanf("%lld %lld %lld", &x, &y, &n);
        printf("%lld\n", (n - y) / x * x + y);
    }
    return 0;
}
