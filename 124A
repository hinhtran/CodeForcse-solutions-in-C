#include<stdio.h>

int max(int a,int b)
{
    if(a >= b) return a;
    else return b;
}
int main()
{
    int n,a,b,ans;
    scanf("%d %d %d",&n,&a,&b);
    ans = n+1 - max(a+1, n-b);
    printf("%d\n", ans);
    return 0;
}
