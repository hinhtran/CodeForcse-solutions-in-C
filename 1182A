#include<stdio.h>

int main()
{
    long long int n;
    scanf("%lld", &n);
    long long int tmp = 1 << (n / 2);
    if(n % 2)
        printf("0\n");
    else
        printf("%lld\n", tmp);
    return 0;
}
