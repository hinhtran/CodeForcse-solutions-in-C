#include<stdio.h>
int main()
{
    int num, total = 0,a , b, c;
    scanf("%d\n", &num);
    while(--num)
    {
        scanf("%d %d %d\n", &a, &b, &c);
        if(a + b + c > 1)
            total++;
    }
    printf("%d\n", total);
    return 0;
}
