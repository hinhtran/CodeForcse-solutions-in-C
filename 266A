#include<stdio.h>

int main()
{
    int n,cnt = 0;
    char s[50];
    scanf("%d \n%s", &n, s);
    for(int i = 0; i < n-1; i++)
    {
        if(s[i] == s[i+1])
            cnt++;
    }
    printf("%d\n", cnt);
    return 0;
}
