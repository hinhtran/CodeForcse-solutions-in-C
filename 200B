#include <stdio.h>

int main()
{
    int n, pi;
    scanf("%d", &n);
    double sum = 0.0;
    for(int i = 0; i < n; i++)
    {
        scanf("%d", &pi);
        sum += pi;
    }
    printf("%.12lf", sum / n);
    return 0;
}
