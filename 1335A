#include<stdio.h>

int main()
{
    int t, n;
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d", &n);
        int ans;
        if(n % 2 == 0)
            ans = n / 2 - 1;
        else
            ans = n / 2;
        printf("%d\n", ans);
    }
    return 0;
}
